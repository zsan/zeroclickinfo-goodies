---
before_install:
  - cpanm Dist::Zilla
  - dzil authordeps | grep -ve '^\W' | xargs cpanm
install:
  - dzil listdeps | grep -ve '^\W' | xargs cpanm
  - cpanm App::DuckPAN
  - duckpan DDG
language: perl
perl:
  - 5.16
  - 5.18
script:
  - dzil smoke --release --author
